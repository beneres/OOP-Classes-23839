using System;namespace exercise2_4{    class Program    {        static void Main(string[] args)        {            //Create a console - based program whose Main() method prompts the user for an integer value and, in turn,            //passes the value to a method that squares the number and to a method that cubes the number.The Cube() method            //should call the Square() method.The Main() method displays the results returned from each of the other methods.            Console.Write("Enter a number to square root : ");            int x;            x = Convert.ToInt32(Console.ReadLine());            squareNumber(x);        }            static void squareNumber(int x)        {            Console.WriteLine("The square root is : " + Math.Sqrt(x));            Console.Write("Enter a number to cubic root : ");            int y;            y = Convert.ToInt32(Console.ReadLine());            cubeNumber(y);        }             static void cubeNumber(int y)         {            Console.WriteLine("The cubic root is : " + Math.Cbrt(y));        }        }    }